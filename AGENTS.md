# 项目规范

## 项目结构

- 仓库包含多个相互独立的浏览器插件；每个插件目录独立维护扩展配置、源代码与测试。
- 跨插件的通用规则维护在根目录 `AGENTS.md`；插件专属约束应维护在对应插件目录中。

## Apple 项目标识

- macOS 应用统一使用 `dev.kevinstack` 作为 Xcode Organization Identifier。
- Bundle Identifier 使用 `dev.kevinstack.<产品标识>` 格式；产品标识使用稳定的小写英文，不包含空格。
- 每个 macOS 项目的完整 Bundle Identifier 必须同时记录在项目配置和对应 Spec 中。

## 操作批准

- 任何会改变文件、代码、配置、静态资源、测试、Git 状态、Issue 或外部系统状态的操作，均须先取得用户的明确批准。
- 用户未明确要求的变更不得基于推断、自主决策或“顺手优化”执行。
- 批准仅覆盖用户明确说明的目标与范围；范围、预期效果或实现方式不明确时，必须先说明计划并等待用户确认。

## 开发流程与事实来源

- 本仓库只使用 Git 管理的本地文档进行项目管理，不依赖外部项目管理系统。
- `docs/changes/README.md` 是工作台，负责想法收件箱、当前唯一活动 Issue、Issue 队列和已完成索引。
- `docs/changes/issues/<工作项>-issue.md` 是任务目标、范围、排除项、阶段、验收标准和状态的唯一事实来源。
- 除纯问答和只读调查外，任何准备修改仓库的独立工作都必须在用户批准范围后先创建 Issue 和 Commit 记录；不得用聊天 Todo 代替正式工作项。
- 每份 Issue 创建时必须同步创建 `docs/changes/commits/<工作项>-commit.md`；两者一一对应并使用相同文件名前缀。Commit 记录先保持待填写状态，实施过程中逐步补充，最终只记录实际交付和验收结果。
- Spec 和 Plan 按需保存在 `docs/superpowers/specs/` 与 `docs/superpowers/plans/`，并纳入 Git 管理。Spec 解释设计选择和边界，Plan 说明实施顺序、影响范围、验证方式和停止点。
- 简单任务可以只使用 Issue 中的 3–6 个 Todo，不强制创建 Spec 或 Plan。存在真实设计分歧、架构边界或多阶段依赖时才创建对应文档。
- Issue、Commit 记录、Spec 和 Plan 必须使用 `YYYY-MM-DD-<project>-<topic>` 公共前缀互相关联，不使用 Commit SHA 作为文档索引。
- 同一时间最多只有一个活动 Issue。当前 Issue 的最终交付 Git Commit 成功前，不得开始下一个 Issue；新想法只能进入工作台收件箱或队列。
- Plan 不得增加 Issue 未批准的功能、依赖或验收范围。实施中需要改变范围时必须暂停，先取得用户批准，再同步 Issue、Spec、Plan 和 Commit 记录。
- 文档发生冲突时，以用户最后明确批准的内容为准，并立即同步到当前 Issue；不得让聊天记录或代理内部计划成为唯一正式依据。

## 模型选择提示

- 规划、架构设计、任务拆解和复杂问题分析阶段优先使用高级模型。
- 进入按既定计划进行的逐文件实现、常规测试和机械性文档更新阶段，可切换到较低成本模型。
- 每次开始实际开发阶段时，先提醒用户确认是否需要从高级模型切换到较低成本模型；模型切换由用户决定，不因未切换而阻塞已批准的工作。

## 用户交互

- 阶段实现和自动化验证完成后，先停在阶段验收，不主动创建 Git Commit、关闭 Issue 或进入依赖阶段。
- 阶段验收说明必须明确写出：本阶段完成了什么、自动化验证和实际结果如何、哪些事项仍未验证或存在限制、用户需要执行什么操作，以及可复现的验收步骤。
- 验收步骤应按用户可观察的结果描述。涉及扩展功能时至少说明要加载的构建产物、核心流程、边界行为和预期结果；纯文档或配置任务则说明需要检查的文件、规则和验证结果。不得把“测试通过”表述为已经完成用户验收。
- 用户明确表示验收通过后，才可把 Issue 和 Commit 记录更新为“待提交”；用户提出问题或表示不通过时，继续处理当前 Issue。
- 功能验收与 Git Commit 批准是两个独立门槛。验收通过不代表允许执行 `git commit`。
- 当用户尚未明确表示验收通过，却要求“提交代码”“整理 Commit”或执行 Git Commit 时，必须先提醒用户完成验收并停在验收阶段；不得把提交请求本身解释为验收通过。用户明确确认验收通过后，仍需单独取得 Git Commit 批准。
- 最终提交前必须展示待提交文件、diff 摘要、验证结果、未验证事项和完整 Commit message。只有用户再次明确批准后才能执行 `git commit`。
- 用户批准后，只有审批请求中已明确列出的机械性终态更新（例如把批准状态改为“已批准”、把当前 Issue 改为“已完成”）可以在同一次提交前执行；任何其他文件或 diff 变化都会使原批准失效，必须重新展示并取得批准。

## 任务规模与拆解

- 任务拆解的目的是明确用户可观察的交付结果，不是增加流程、文档、抽象、测试或阶段数量。进度以用户目标的完成程度衡量，不以 Issue、文档、测试、文件或工具数量衡量。
- 实施前必须先按用户价值和风险判断规模：单一用户目标且不改变公共 API、数据模型、权限、构建体系或核心架构的属于小型任务；同一交付结果包含 2–3 个有依赖关系的实施阶段属于中型任务；涉及跨项目公共能力、公共 API、数据模型、权限、安全、构建体系或不可逆迁移的才属于架构任务。
- 小型任务默认使用一个 Issue，不创建子 Issue，没有真实设计分歧时不编写独立 Spec，实施说明以 3–6 个步骤为宜。不得仅因涉及多个文件、组件或测试就升级为架构任务。
- Sub-issue 只用于同一交付结果内部存在真实依赖的实施阶段；能够独立产生价值、验收或回滚的结果必须拆成新的主 Issue。修改不同文件、实现不同组件、增加测试、删除旧代码或执行最终验证，默认都只是 Todo。
- Issue 记录问题、用户目标、范围、排除项、当前阶段和可验证的验收标准。Spec 只记录必须作出的设计决定、关键边界与取舍。Plan 只记录实施顺序、影响模块、必要验证和停止点，不得包含完整实现代码、完整测试代码或重复 Issue 的大段内容。
- 小型任务如确需 Plan，默认不超过 120 行。超过前必须先向用户说明无法继续简化的原因并取得批准。
- 实施顺序默认为：保持核心行为 → 完成主路径 → 删除被替代的旧实现 → 补充与真实风险对应的测试 → 执行一次完整验证 → 用户验收。不得先穷举理论边界再开始核心实现。
- 不得主动增加当前目标不需要的抽象、组件层、依赖、架构守卫或未来扩展能力。发现新问题时，先判断是否阻塞当前用户验收；不阻塞时只记录为后续事项，不扩大当前范围。
- 如果连续两轮工作没有推进用户可观察结果，必须停止当前路径并重新评估，不得继续堆叠细节。

## 验证、资源与熔断

- 测试必须对应用户行为或真实架构边界，不得大量测试源码文本、导入形式、组件内部结构或其他实现细节。同一行为只保留一个主要验证层级，已有稳定集成覆盖时不再增加重复的低层测试。
- 未经用户明确批准，不得引入假 DOM 环境。依赖 Portal、真实焦点、指针事件、布局测量或视口碰撞的交互，必须在 Chrome 或 Edge 中验证，不得为模拟第三方组件内部行为补写大量浏览器 API 桩。
- 针对性测试通过后，只在阶段完成时运行一次完整测试、类型检查和构建。没有相关代码变化时不得重复执行全量验证。
- 失败后必须先判断它是否阻塞核心用户行为、是否属于当前验收标准，以及是否只是测试框架、模拟环境或工具问题。不阻塞核心行为时，记录限制并继续主线，不得为了满足非关键测试而修改正常业务代码。
- 同一失败最多允许一次原因定位和一次修复重试。第二次仍以相同方式失败时，必须停止循环，向用户说明已确认事实、对核心功能的影响、替代验证方式与需要用户决定的事项。
- 禁止启动子代理或并行执行多个任务。所有命令必须串行执行，一次只运行一个。
- 开发阶段不执行视觉、布局或交互验收；这些内容由用户自行在 Chrome/Edge 中验收。代理只验证纯逻辑、配置、构建和必要架构边界。
- 现有测试中凡是截图、样式像素、布局尺寸、主题外观、Tooltip/菜单交互、键盘/焦点/指针流程的测试全部移除；不得新增同类测试。
- 不得整份读取、复制或输出大文件；只读取当前决策所需的相关片段，并为命令输出设置合理上限。
- 不启动不必要的 watch、dev server 或长期后台进程。单个测试出现卡死、异常内存增长或超出该项目历史正常时长时，必须立即终止。
- 出现系统内存告警时，必须立即停止所有命令、后台进程和自动重试，只向用户汇报当前状态。未经用户明确允许，不得继续启动任何进程。
- 不得通过增加代理、扩大上下文、重复运行测试或加入更多抽象来解决进度问题。

## 编码规范

- 所有文本文件必须使用 UTF-8 编码。
- 所有代码注释必须使用中文。

## 版本管理

- Node.js、包管理器、构建工具以及第三方依赖必须使用完整、精确的版本号。
- `package.json` 中不得使用 `^`、`~`、`*`、`latest`、`next` 或其他浮动版本与版本范围。
- 依赖升级必须由明确任务触发，并同步更新所有相关 `package.json`、`pnpm-lock.yaml` 与版本管理配置；更新后必须重新执行安装、构建和测试验证。

## 自动化测试规范

- 目录迁移前必须保留并调整既有行为测试，不得因迁移删除测试覆盖。
- 测试按页面或功能组件组织；测试文件应与被测功能的职责对应。

## 浏览器兼容性

- 所有插件仅面向基于 Chromium 的 Chrome 和 Edge；不要求兼容 Firefox、Safari 或其他浏览器。
- 新增浏览器 API、Manifest 配置或扩展能力时，必须确认其在当前稳定版 Chrome 与 Edge 中可用；必要时提供功能检测与明确的降级行为。
- 插件功能变更完成后，视觉与交互由用户自行在 Chrome 或 Edge 中实际加载验收；代理开发阶段不执行该类验证，也不得将其作为自动化测试要求。

## 扩展图标规范

- 插件 Logo 以用户提供的 SVG 为唯一源稿；除非用户明确要求，不得重绘、重组、增删路径或改变图案细节，只能进行裁边、等比缩放和颜色转换。
- 图标 PNG 在导出前必须裁掉多余的透明留白；不得将包含大面积透明边距的原图直接缩放为图标。
- 保持图案原始比例，不得拉伸或裁切主体；在 `128×128` 图标中，主体最长边应尽可能填满画布，并保留约 `2px` 安全边距。
- `16`、`32`、`48`、`128` 四种图标尺寸必须使用相同构图，并由裁边后的源图生成。
- 图标必须使用透明背景、纯色线条或图案；不得添加底板、渐变、阴影、纹理或其他未获明确批准的视觉效果。
- 默认图标使用黑色；系统深色模式使用同构的白色版本。颜色转换只能改变颜色，不得改变透明度、构图或图案比例。
- 同一插件的默认黑色资源命名为 `<插件名>-<尺寸>.png`，深色模式白色资源命名为 `<插件名>-light-<尺寸>.png`。

## Issue 规范

- Issue 文件命名为 `YYYY-MM-DD-<project>-<topic>-issue.md`，标题使用简洁的中文动作与目标。
- Issue 必须包含元信息、背景、目标、范围、排除项、Todo 或 Sub-issues、验收标准、关联文档和唯一下一步。
- Issue 是一次可以整体验收、形成一个最终交付 Git Commit 并独立回滚的交付单元。
- Sub-issue 只在主 Issue 文件内部定义有顺序的阶段，不创建独立文件，也不对应独立 Commit 记录或 Git Commit。
- 如果某部分能够独立产生价值、独立验收或独立回滚，即使后续不做也成立，则应拆成新的主 Issue，而不是 Sub-issue。
- 修改不同文件、增加测试、删除旧实现或执行验证只是 Todo，不作为拆分 Issue 的理由。
- 一个 Issue 通常包含 1–3 个 Sub-issue；超过 3 个时应优先判断是否需要拆成多个顺序 Issue。
- Todo 创建时使用 `- [ ]`，实际完成并验证后改为 `- [x]`。不得预先标记完成。
- Issue 不维护优先级、标签、负责人、截止日期或远程系统标识。

## Commit 记录规范

- Commit 记录文件命名为 `YYYY-MM-DD-<project>-<topic>-commit.md`，并在创建 Issue 时同步创建。
- Commit 记录必须链接唯一对应的 Issue；Issue 也必须反向链接 Commit 记录。
- 初始 Commit 记录只填写预期交付边界和待完成栏目，不得把计划描述成实际结果。
- 实施过程中可以补充实际完成内容、验证结果、未验证事项和限制，但用户验收状态与最终提交批准状态必须保持真实。
- Commit 记录只对应最终交付 Git Commit，不记录可选的规划 Commit，不保存 Commit SHA，也不复制完整 Spec 或 Plan。
- 最终 Commit message 必须在提交前写入 Commit 记录并展示给用户审核。

## 阶段验收与 Git 提交

- 一个 Issue 原则上对应一个最终交付 Git Commit；内部 Sub-issue 只是实施阶段，不单独提交。
- 复杂 Issue 可以在 Spec 和 Plan 获得批准后创建一次规划 Commit，但必须由用户单独批准；`-commit.md` 仍只对应最终交付 Commit。
- 开发完成后必须先运行 Issue 约定的验证并停在用户验收。自动化通过不等于用户验收通过，用户验收通过也不等于最终提交获批。
- Issue、Commit 记录和工作台的“已完成”终态必须作为最终交付 Commit 的一部分；只有该 Commit 成功后，这些终态才成为仓库事实并允许开始下一个 Issue。
- 最终审批请求必须提前列出随提交发生的终态字段更新。Git Commit 失败时应立即把工作区中的相关字段恢复为“待提交”和“待批准”，不得留下虚假的完成状态。
- 未经用户明确要求，不得执行 `git commit --amend`、交互式 rebase、squash、force push 或自动推送远端。

## Git Commit 规范

- 使用 Conventional Commits 格式：
  `<type>(<scope>): <中文动作与结果>`。
- `type` 只能使用：
  `feat`、`fix`、`refactor`、`test`、`docs`、`chore`、`build`、`ci`、`perf`、`revert`。
- 类型选择：
  - `feat`：新增用户功能
  - `fix`：修复用户可观察的问题
  - `refactor`：不改变行为的代码重构
  - `chore`：依赖、工具、配置或仓库维护
  - `docs`：文档变更
  - `test`：测试变更
- `scope` 使用稳定的小写项目名，如 `my-tabs`、`repo`。
- 一个 Commit 只对应一个可独立验收和回滚的结果。
- 不同类型或不同用户结果必须拆分为多个 Commit。
- 标题使用中文动作描述结果，不写文件名，不使用“更新若干文件”等模糊表述，末尾不加句号。
- 正文控制在 1-3 条中文项目符号，只描述实际变更结果。

### 推荐格式

```text
fix(my-tabs): 修复深色模式首屏白闪

- 内联新标签页明暗模式背景
- 消除页面首次加载时的白色闪烁
```

## 稳定开发产物

- 基于 WXT 的扩展开发时，浏览器不得直接加载会在构建开始时被清空的 `dist/<browser>-mv<manifest>-dev` 临时目录。
- 公共稳定发布工具位于 `packages/stable-extension-dev`；WXT 子项目在 `wxt.config.ts` 中接入 `createStableDevelopmentHooks()`，不自行实现复制、校验或目录替换逻辑。
- 开发命令必须保持项目原有用法不变；公共 hook 在 WXT `build:done` 成功后，将完整产物发布到同级 `dist/<browser>-mv<manifest>-dev-stable` 目录，浏览器只加载该稳定目录。
- 发布前必须确认 `manifest.json` 可解析、Manifest 字段完整，并确认构建输出报告中的全部文件存在；校验或构建失败不得删除或覆盖上一份稳定产物。
- 稳定目录更新必须通过同文件系统的暂存目录和目录替换完成；Windows 文件占用或替换异常时必须保留旧稳定目录并返回可诊断错误。
- 公共工具必须有自动化测试覆盖成功发布、失败保留旧产物、无效或不完整产物拒绝发布和 Windows 替换异常；接入扩展还必须完成 Chrome 或 Edge 的成功→失败→恢复成功实际加载验证。
