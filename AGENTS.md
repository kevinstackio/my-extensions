# 项目规范

## 项目结构

- 仓库包含多个相互独立的浏览器插件；每个插件目录独立维护扩展配置、源代码与测试。
- 跨插件的通用规则维护在根目录 `AGENTS.md`；插件专属约束应维护在对应插件目录中。

## 操作批准

- 任何会改变文件、代码、配置、静态资源、测试、Git 状态、Issue 或外部系统状态的操作，均须先取得用户的明确批准。
- 用户未明确要求的变更不得基于推断、自主决策或“顺手优化”执行。
- 批准仅覆盖用户明确说明的目标与范围；范围、预期效果或实现方式不明确时，必须先说明计划并等待用户确认。

## 编码规范

- 所有文本文件必须使用 UTF-8 编码。
- 所有代码注释必须使用中文。

## 版本管理

- Node.js、包管理器、构建工具以及第三方依赖必须使用完整、精确的版本号。
- `package.json` 中不得使用 `^`、`~`、`*`、`latest`、`next` 或其他浮动版本与版本范围。
- 依赖升级必须由明确任务触发，并同步更新所有相关 `package.json`、`pnpm-lock.yaml` 与版本管理配置；更新后必须重新执行安装、构建和测试验证。

## 自动化测试规范

- 目录迁移前必须保留并调整既有行为测试，不得因迁移删除测试覆盖。
- 测试按页面或功能组件组织；测试文件应与被测功能的职责对应。

## 浏览器兼容性

- 所有插件仅面向基于 Chromium 的 Chrome 和 Edge；不要求兼容 Firefox、Safari 或其他浏览器。
- 新增浏览器 API、Manifest 配置或扩展能力时，必须确认其在当前稳定版 Chrome 与 Edge 中可用；必要时提供功能检测与明确的降级行为。
- 插件功能变更完成后，须至少在 Chrome 或 Edge 中完成一次实际加载与核心流程验证。

## 扩展图标规范

- 插件 Logo 以用户提供的 SVG 为唯一源稿；除非用户明确要求，不得重绘、重组、增删路径或改变图案细节，只能进行裁边、等比缩放和颜色转换。
- 图标 PNG 在导出前必须裁掉多余的透明留白；不得将包含大面积透明边距的原图直接缩放为图标。
- 保持图案原始比例，不得拉伸或裁切主体；在 `128×128` 图标中，主体最长边应尽可能填满画布，并保留约 `2px` 安全边距。
- `16`、`32`、`48`、`128` 四种图标尺寸必须使用相同构图，并由裁边后的源图生成。
- 图标必须使用透明背景、纯色线条或图案；不得添加底板、渐变、阴影、纹理或其他未获明确批准的视觉效果。
- 默认图标使用黑色；系统深色模式使用同构的白色版本。颜色转换只能改变颜色，不得改变透明度、构图或图案比例。
- 同一插件的默认黑色资源命名为 `<插件名>-<尺寸>.png`，深色模式白色资源命名为 `<插件名>-light-<尺寸>.png`。

## Issue 规范

- 默认不创建或更新 Issue；仅当用户明确要求时执行。
- Issue 标题格式为 `【<子项目显示名称>】<简洁的动作与目标>`。
- 子项目显示名称以 Linear 项目 `Browser Extensions` 中已有同类 Issue 的标题写法为准，保持大小写与空格一致。
- 本仓库的 Issue 创建或更新时必须关联 Linear 项目 `Browser Extensions`；主 Issue 与全部子 Issue 均须关联该项目。
- 正文以可独立验证的 Markdown 任务清单为主；必要时先用一两句话说明背景或视觉参考。
- 创建时使用 `- [ ]`；实现并验证完成后改为 `- [X]`。
- 每项清楚描述变更、边界条件或验证结果；涉及路径、配置或技术约束时使用行内代码标明。
- 默认不设置优先级和标签，除非任务另有明确要求。
- 跨多个可独立完成步骤的目标应创建主 Issue，并为每个可独立实现、验证或关闭的步骤创建子 Issue。
- 每个主 Issue 原则上最多包含 3 个子 Issue；超过 3 个时必须按可独立验收的阶段拆分为多个主 Issue，避免主 Issue 过大、后续接入和验收困难。
- 主 Issue 说明整体目标、范围和最终验收标准；使用清单链接或列出子 Issue，并以子 Issue 全部完成作为关闭条件。
- 子 Issue 聚焦单一职责，标题沿用 `【<子项目名称>】` 格式，正文只描述该步骤的实现与验收要求。
- 子 Issue 如涉及用户检查、浏览器实际加载、视觉验收或其他需用户确认的动作，必须在用户明确验证通过后才能关闭；依赖该验收的后续步骤不得开始。
- 主 Issue 即使全部子 Issue 和自动化测试均已完成，也必须在用户明确最终审核通过后才能关闭。
- 在 Issue 清单中明确区分中间验收与最终验收：中间验收未通过时不得进入依赖该结果的下一步；最终验收通过后才能关闭对应主 Issue。

## 稳定开发产物

- 基于 WXT 的扩展开发时，浏览器不得直接加载会在构建开始时被清空的 `dist/<browser>-mv<manifest>-dev` 临时目录。
- 公共稳定发布工具位于 `packages/stable-extension-dev`；WXT 子项目在 `wxt.config.ts` 中接入 `createStableDevelopmentHooks()`，不自行实现复制、校验或目录替换逻辑。
- 开发命令必须保持项目原有用法不变；公共 hook 在 WXT `build:done` 成功后，将完整产物发布到同级 `dist/<browser>-mv<manifest>-dev-stable` 目录，浏览器只加载该稳定目录。
- 发布前必须确认 `manifest.json` 可解析、Manifest 字段完整，并确认构建输出报告中的全部文件存在；校验或构建失败不得删除或覆盖上一份稳定产物。
- 稳定目录更新必须通过同文件系统的暂存目录和目录替换完成；Windows 文件占用或替换异常时必须保留旧稳定目录并返回可诊断错误。
- 公共工具必须有自动化测试覆盖成功发布、失败保留旧产物、无效或不完整产物拒绝发布和 Windows 替换异常；接入扩展还必须完成 Chrome 或 Edge 的成功→失败→恢复成功实际加载验证。