# 实现 X Download 公开视频自动下载

## 元信息

- 工作项：`2026-09-20-x-download-public-video-download`
- 项目：`x-download-helper`
- 类型：架构任务
- 状态：已排队
- 当前阶段：等待前置工具链工作项完成并提交
- 创建日期：2026-09-20
- 最近更新：2026-09-20

## 背景

扩展已经能把 X 单帖地址交给 Helper，固定视频工具链由前置工作项负责。本工作项只交付正常网络下的公开视频自动下载主路径。

## 目标

用户在公开 X 单帖页面点击插件后，Helper 自动解析该帖全部视频，按单任务串行队列下载最高可用画质、合并音视频并保存到桌面，不需要再次点击“下载”。

## 范围

- 插件继续只传递规范化帖子地址和 `postId`，由 Bridge 在需要时唤起 Helper。
- Helper 使用内置工具解析公开帖子中的全部视频，不读取 Cookie。
- 同一时刻只运行一个下载进程，多个帖子和视频按队列顺序处理。
- 使用临时工作目录完成下载与合并，成功后移动到桌面。
- 文件按接收时间命名为 `X_VIDEO_yyyyMMdd_HHmmss.mp4`；多视频增加 `_01`、`_02` 顺序号。
- 当前内存队列以规范化帖子地址去重；任务移除后允许重新下载。
- 展示等待、解析、下载、合并、完成和基础失败状态。
- 删除初始手动“下载”按钮，任务收到后直接开始。

## 排除项

- 不处理图片、Cookie、私密帖子、受限内容或付费内容。
- 不实现取消、重试、失败删除、部分失败恢复或断点续传。
- 不检查桌面既有文件，不维护下载历史或持久化去重。
- 不提供清晰度、目录、并发数或命名设置。
- 不处理 Windows、签名、公证、安装器或对外分发。

## Todo

- [ ] 前置工具链工作项完成并提交后，补齐并批准本 Issue 的 Spec 与 Plan。
- [ ] 实现公开视频解析、串行队列和内存地址去重。
- [ ] 实现临时目录、最高画质下载、音视频合并和桌面落盘。
- [ ] 接入 Helper 任务状态并移除手动下载按钮。
- [ ] 完成正常主路径自动化验证和 macOS 实际验收。

## 验收标准

- 单视频公开帖子可在一次插件点击后自动下载到桌面。
- 多视频公开帖子生成 `_01`、`_02` 等独立文件，并按顺序处理。
- 连续提交多个帖子时保持单任务串行执行，不丢失任务。
- 当前队列已有相同规范化地址时不创建重复任务，只展开现有 Helper。
- 下载使用最高可用画质，分离的音视频由内置 FFmpeg 合并，不主动转码。
- 正常下载过程中不依赖 Cookie、Homebrew、Python 或系统工具路径。

## 关联文档

- [Commit 记录](../commits/2026-09-20-x-download-public-video-download-commit.md)
- 前置 Issue：[打包 X Download Helper 固定视频工具链](./2026-09-20-x-download-helper-toolchain-issue.md)

## 唯一下一步

等待前置工具链工作项完成最终 Git Commit。
